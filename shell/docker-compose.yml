version: '3.7'

services:
  btc_monitor:
    image: leandro2r/btc_monitor:latest-shell
    privileged: true
    devices:
      - /dev/snd:/dev/snd
    volumes:
      - .:/home/btc_monitor
    entrypoint: "tail -f /dev/null"
